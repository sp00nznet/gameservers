# Silverware Game Server Deployer Configuration

# Flask secret key (generate a random string for production)
SECRET_KEY=your-secret-key-here

# Database URL (SQLite by default)
DATABASE_URL=sqlite:///deployer.db

# Flask environment
FLASK_ENV=development
FLASK_DEBUG=1

# Server binding
HOST=0.0.0.0
PORT=5000
